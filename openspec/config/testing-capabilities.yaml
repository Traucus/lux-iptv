# Testing Capabilities - Lux IPTV
# Generated by sdd-init on 2026-08-23

project: iptv
generated_at: "2026-08-23T21:45:00Z"

testing:
  strict_tdd: true
  frameworks:
    unit_integration:
      name: "Vitest"
      command: "vitest"
      coverage_command: "vitest --coverage"
      config_file: "vitest.config.ts"
      status: "not_installed"
    e2e:
      name: "Playwright"
      command: "playwright test"
      config_file: "playwright.config.ts"
      status: "not_installed"
      electron_support: true
    linter:
      name: "ESLint"
      command: "eslint src/"
      config_file: ".eslintrc.js"
      status: "not_installed"
    type_checker:
      name: "TypeScript"
      command: "tsc --noEmit"
      config_file: "tsconfig.json"
      status: "not_installed"
    formatter:
      name: "Prettier"
      command: "prettier --write src/"
      config_file: ".prettierrc"
      status: "not_installed"

test_layers:
  unit:
    description: "Individual function/component testing"
    coverage_threshold: 80
    examples:
      - "M3U parser heuristics"
      - "Name preprocessing pipeline"
      - "TMDB client functions"
      - "Focus navigation logic"
  integration:
    description: "Module interaction testing"
    examples:
      - "Web Worker ↔ IndexedDB communication"
      - "HLS.js error recovery flow"
      - "EPG linking with playlist data"
  e2e:
    description: "Full user flow testing with Playwright"
    examples:
      - "Profile selection → Home navigation"
      - "Playlist ingestion → content display"
      - "Parental control PIN validation"
    electron_tests:
      - "App startup and HWID extraction"
      - "License validation flow"
      - "Multi-window management"

performance_tests:
  - id: "TEST-01"
    name: "UI thread blocking"
    metric: "55 FPS minimum, <50ms blocking"
  - id: "TEST-02"
    name: "Channel zapping latency"
    metric: "<2 seconds audio/video load"
  - id: "TEST-07"
    name: "EPG parsing speed"
    metric: "<500ms for 100K programs"
  - id: "TEST-08"
    name: "Memory management"
    metric: "Auto-purge at 80% RAM"

security_tests:
  - id: "TEST-03"
    name: "Parental control enforcement"
  - id: "TEST-06"
    name: "License validation blocking"

ci_cd:
  suggested_workflow:
    - "Run linting on PR"
    - "Run type checking"
    - "Run unit tests with coverage"
    - "Run integration tests"
    - "Run Playwright E2E tests"
    - "Performance regression checks"

notes:
  - "Electron testing requires special setup for main process"
  - "Web Worker testing may need vitest worker polyfills"
  - "HLS.js testing requires mock streams"
  - "OPFS testing requires secure context"